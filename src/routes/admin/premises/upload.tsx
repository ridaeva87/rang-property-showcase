import { createFileRoute } from "@tanstack/react-router";
export const Route=createFileRoute("/admin/premises/upload")({server:{handlers:{POST:async({request})=>{try{const f=await request.formData(),file=f.get("file");if(!(file instanceof File))throw new Error("Файл не выбран");await (await import("@/server/admin/admin.server")).uploadPremiseMediaAdmin({premiseId:String(f.get("premiseId")),role:String(f.get("role"))==="video"?"video":"photo",file});return new Response(null,{status:303,headers:{location:"/admin/premises"}})}catch(e){return new Response(e instanceof Error?e.message:"Ошибка",{status:400})}}}}});
