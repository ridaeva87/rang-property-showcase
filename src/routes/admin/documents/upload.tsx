import { createFileRoute } from "@tanstack/react-router";
export const Route=createFileRoute("/admin/documents/upload")({server:{handlers:{POST:async({request})=>{try{const origin=request.headers.get("origin");if(origin&&!(["https://rangpro.ru","https://www.rangpro.ru"].includes(new URL(origin).origin)))return new Response("Доступ запрещён",{status:403});const f=await request.formData();const file=f.get("file");if(!(file instanceof File))throw new Error("Файл не выбран");const {savePrivateDocumentAdmin}=await import("@/server/portal/portal.server");const date=String(f.get("date")||"");await savePrivateDocumentAdmin({userId:String(f.get("userId")),title:String(f.get("title")),type:String(f.get("type")),...(date?{date}:{}),file});return new Response(null,{status:303,headers:{location:"/admin/documents?uploaded=1"}})}catch(e){return new Response(e instanceof Error?e.message:"Ошибка",{status:400})}}}}});
