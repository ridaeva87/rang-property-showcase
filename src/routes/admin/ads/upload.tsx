import{createFileRoute}from"@tanstack/react-router";export const Route=createFileRoute("/admin/ads/upload")({server:{handlers:{POST:async({request})=>{try{const f=await request.formData(),file=f.get("file"),role=String(f.get("role"));if(!(file instanceof File))throw new Error("Файл не выбран");if(!["logo","banner","gallery"].includes(role))throw new Error("Неверный тип изображения");await(await import("@/server/ads/ads.server")).uploadAdMediaAdmin({placementId:String(f.get("placementId")),role:role as "logo"|"banner"|"gallery",file});return new Response(null,{status:303,headers:{location:"/admin/ads"}})}catch(e){return new Response(e instanceof Error?e.message:"Ошибка",{status:400})}}}}});
