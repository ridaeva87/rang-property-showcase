import{describe,expect,it}from"vitest";import{hasPermission,requestDirectionScope}from"@/server/portal/portal.server";
describe("employee RBAC",()=>{it("keeps administrator unrestricted",()=>{expect(hasPermission(["admin"],[],"employees.manage")).toBe(true);expect(requestDirectionScope(["admin"])).toBeNull()});it.each([["accountant","accounting"],["lawyer","legal"],["technician","technical"]])("limits %s to its request direction",(role,direction)=>{const scope=requestDirectionScope([role]);expect(scope).toContain(direction);expect(scope).not.toContain("management");expect(scope).not.toContain(direction==="accounting"?"legal":"accounting")});it("limits rental manager and rejects absent permissions",()=>{expect(requestDirectionScope(["rental_manager"])).toEqual(["management","access","documents","other"]);expect(hasPermission(["rental_manager"],["premises.manage"],"documents.manage")).toBe(false)})});
