# PostgreSQL connection string. Never commit the real value.
DATABASE_URL=postgresql://rang_app:change-me@127.0.0.1:5432/rang

# Keep the pool deliberately small on the current VPS; tune only after measurement.
DATABASE_POOL_MAX=10

# Optional application environment marker used by operational tooling.
APP_ENV=development
